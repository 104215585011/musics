# Claudio FM — 评审评分表

> 一轮（或几轮）会话后使用。按六个维度打分，每项 0-2 分，满分 12 分。

---

## 评分维度

### 1. 正确性（0-2）
实现出来的行为是否符合 `feature_list.json` 中的 `user_visible_behavior`？

| 分 | 标准 |
|----|------|
| 2 | 功能行为完全符合描述，边界情况也处理正确（如无歌词时的 fallback、NetEase 失败时的错误状态） |
| 1 | 主路径正确，但边界情况有遗漏（如无 URL 时 UI 未正确显示错误 pill） |
| 0 | 主路径行为与描述不符，或引入了回归 bug |

**Claudio FM 专项检查：**
- [ ] Electron 端改动未破坏 PWA 端（两端分别验证）
- [ ] 错误状态（无播放 URL）显示正确
- [ ] 歌词高亮跟随播放时间准确
- [ ] 聊天 AI 回复意图匹配正确（点播/跳过/询问）

---

### 2. 验证（0-2）
要求的验证步骤是否真正执行，并留下证据？

| 分 | 标准 |
|----|------|
| 2 | `npm test` 通过、浏览器验证完成、Electron 验证完成，`feature_list.json` evidence 字段有具体记录 |
| 1 | 测试通过但缺少浏览器/Electron 验证；或 evidence 字段为空 |
| 0 | 仅有代码改动，无任何验证记录；或测试失败被忽略 |

**不可接受：**
- evidence 字段写"已测试"但无具体描述
- 通过注释测试用例来让 `npm test` 通过
- 声称 Electron 验证完成但未运行 `npm run desktop`

---

### 3. 范围纪律（0-2）
是否基本保持在选定功能范围内，未展开额外改动？

| 分 | 标准 |
|----|------|
| 2 | 改动集中在目标功能的相关文件，无计划外的重构或旁路修复 |
| 1 | 有 1-2 处计划外改动，但已在 claude-progress.md 中记录说明 |
| 0 | 大量计划外改动，或同时有多个 `in_progress` 功能 |

**Claudio FM 专项：**
- Electron 样式改动只在 `body.is-electron` 块，未动 PWA 样式
- 未在修 UI 时顺手改后端逻辑（或反之）

---

### 4. 可靠性（0-2）
结果是否在重启或重跑后继续工作？

| 分 | 标准 |
|----|------|
| 2 | `npm start` 重启后功能依然正常；Electron 关闭重开状态正确；`npm test` 可重复通过 |
| 1 | 首次运行正常，但重启后出现问题（如 WebSocket 重连失败、session 丢失） |
| 0 | 功能依赖特定临时状态，无法重现 |

**Claudio FM 专项检查：**
- [ ] NetEase 登录 session 在服务重启后是否需要重新登录（记录预期行为）
- [ ] Electron 窗口位置/尺寸是否正确持久化（window-state.json）
- [ ] 用户 Persona 文件修改保存后，重启服务是否仍然生效

---

### 5. 可维护性（0-2）
代码和文档是否清楚到足以交给下一轮会话？

| 分 | 标准 |
|----|------|
| 2 | 关键改动有注释说明意图；新增 CSS 选择器有作用域注释；`session-handoff.md` 完整填写 |
| 1 | 代码可读但缺少注释；或 handoff 文件填写不完整 |
| 0 | 代码意图不明，下一轮 agent 需要逆向工程才能理解；或无 handoff 文件 |

**Claudio FM 专项：**
- `styles.css` 中新增的 Electron 专属规则应注明 `/* Electron UI redesign: [功能说明] */`
- 复杂的 JS 改动应说明为什么这样做，而不只是写 what

---

### 6. 交接就绪度（0-2）
新会话是否能只凭仓库内文件继续推进？

| 分 | 标准 |
|----|------|
| 2 | `claude-progress.md`、`feature_list.json`、`session-handoff.md` 全部最新；新 agent 读完能立刻开工 |
| 1 | 文件更新了但信息不完整；或"下一步动作"描述不够具体 |
| 0 | 文件未更新；新 agent 无法从文件中判断当前状态 |

---

## 评分汇总

| 维度 | 得分（/2） | 备注 |
|------|-----------|------|
| 正确性 | | |
| 验证 | | |
| 范围纪律 | | |
| 可靠性 | | |
| 可维护性 | | |
| 交接就绪度 | | |
| **总分** | **/12** | |

---

## 结论

- **Accept（≥10 分）**：达标，可进入下一功能
- **Revise（7-9 分）**：需修补后接受，列出具体修补项
- **Block（≤6 分）**：有根本性问题，必须先解决再继续

### 本次结论

```
结论：[ Accept / Revise / Block ]

修补项（Revise 时填写）：
1.
2.

阻断原因（Block 时填写）：

```

---

## 校准说明

Evaluator 需要校准——agent 做评审容易自我说服到通过。校准步骤：

1. 用此表给一个已完成的功能打分
2. 与人工判断对比，找出分歧点
3. 把分歧点的通过/失败标准写得更具体（直接编辑上方表格）
4. 对同一输出重新打分，看对齐了没有
5. 预计需要 3-5 轮才能对齐

*记录每次校准改了什么、为什么改。*
